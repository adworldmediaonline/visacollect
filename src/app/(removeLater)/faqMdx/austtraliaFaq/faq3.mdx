import Link from 'next/link';
export const LinkText = ({ text, href }) => (
  <Link className="text-primary font-semibold text-lg" href={href}>
    {text}
  </Link>
);

You can apply online through the user-friendly platform of <LinkText text="visacollect.com" href="/in/visa/step-one"/>. Simply fill out the application form, upload the required documents, and make the payment.
